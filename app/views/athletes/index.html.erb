<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h1>Athletes</h1>
      <ul>
        <% @athletes.each do |a| %>
          <li><%= link_to a.name, a %></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
